
Voting:

Answer is input. `qna.gd`on answer submitted gets validation result and emits round results isgnal.
The signal is emitted with differing params. In the case of a "long fuzzy" answer (not just a single letter out) the emit signal is sent .

`round_result` is connected in `game_board`. Depending on signal, calls relevant handler.
```gdscript
 GameManager.SubmissionResult.FUZZY:
	await _handle_fuzzy_answer(player, prize, submitted_answer)
```

`_handle_fuzzy_answer` awaits `handle_fuzzy_answer`. 

GameBoardvoteSession:

Need to: 
- make sure the main game shows a vote is happening. A oplace to build tension with the media on screen. For this will need to find more music, do possible VOs etc (longer term issue)
- Give some time between calling the vote and players having to vote. Right now it just happens. Think the timer was 20s (now 25) but if you aren't paying full attention EASY to miss! controller needs to be clearer. 



Double constants: I don't remember why:

GameBoardVoteSession.gd (6)
`const NETWORK_VOTE_TIMEOUT_SECONDS := 25.0`

game_board.gd 9520
`const NETWORK_VOTE_TIMEOUT_SECONDS = 25.0`



Practical structure:

1. One VoteTransitionOverlay scene with:

- title text
- body text
- optional countdown
- optional auto-dismiss

2. One show_phase(phase, data) style API to swap copy/behavior by phase.
3. Keep timing constants in one place (also fix the duplicate vote-timeout constant while doing this).

Tightened “Resume Tomorrow” block:

1. Confirm single source for vote timeout constant.
2. Add vote ceremony phases:

- incoming
- cast
- result

3. Insert short pre-vote delay (about 1.0 to 1.5s) before voting opens.
4. Make controller prompt ultra-clear during cast phase.
5. Verify flow on one phone + one tablet:

- can you tell a vote is starting
- can you tell when input is open
- can you tell outcome instantly

6. If clear, then optional polish pass:

- countdown rhythm
- simple audio cue
- stronger result wording