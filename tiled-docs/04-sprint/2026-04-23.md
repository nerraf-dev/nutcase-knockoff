Question transitions, maybe change the text. Lose "no winner yet starting next round" look to have things like:
"player one is close"
"player 3 has fallen behind"

things like that. And next questions coming up, rather than round. 


New vote transition: 
- probably going to make a questions set with questions that allow for getting fuzzy answwers! Current test didn't have the answers that really needed it most were less than 5 chars.
- When transition appeared - definitely changed the main screen! Clear a vote was happening on the screen, still need to make the controller a bit jazzier. Currently its built like a column with sections that appear and hide. try to make the sections fill the screen. Use chunky buttons etc. 
- Need to look at delaying the 'game end', give it a bit after the winning question. Do the same "Correct - well done" overlay. If there is a winner then maybe do something like play a drum roll and "ooh, looks like we have a winner..." -> GAame End!
- Result of a correct vote says "exact amtch" need to chang ethe output of a vote to the 'close' or potentially a custome message, like "PLAYER-ANSWER, phew! got away with that one!"
- 


---

**Priority Plan**
1. Lock vote incoming tone and structure first.
2. Keep one reusable overlay scene.
3. Move vote copy into a small template list/dictionary.
4. Add lightweight variation + no-repeat behavior.
5. Then tune controller copy/layout.

**Copy System Plan**
1. Define a vote incoming copy pool with placeholders:
- player_name
- submitted_answer
2. Use 4-6 lines max to avoid overdesign.
3. Rotate randomly but avoid immediate repeats.
4. Keep one fallback line if formatting data is missing.

Example style set:
1. "{player_name} answered: \"{submitted_answer}\".\nClose enough for the points?\nVOTE!"
2. "{player_name} said \"{submitted_answer}\".\nYou taking that?"
3. "\"{submitted_answer}\" from {player_name}.\nDoes it count?"
4. "{player_name} dropped an answer.\nClose enough?\nCast your vote."

**Ceremony Beats (keep)**
1. Incoming (short lead-in, dynamic copy)
2. Vote active (countdown + rule reminder)
3. Vote result (accepted/rejected/tie)



**Test Plan For This Copy Pass**
1. Run 3 fuzzy votes in one session.
2. Check each incoming message reads clearly at a glance.
3. Confirm no repeated line back-to-back.
4. Confirm answer/player interpolation always renders cleanly.
